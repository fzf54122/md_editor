# Testing & Validation

本项目当前主要依赖手动测试来验证 Typora 风格 Markdown 编辑体验。以下清单帮助你在每次提交或发布前快速回归关键功能。

## 环境准备
1. 构建 Release/Debug 任一版本：
```bash
cmake -S . -B build
cmake --build build
```
2. 运行应用：./build/md_editor
> 目前暂无自动化 GUI 测试用例；如需集成 CI，可在未来补充 Qt Test / Squish 等方案。

## 手动测试清单

### 1. 基础编辑 & Markdown 渲染
| # | 步骤 | 期望结果 |
| --- | ------ | ---------- |
| 1 | 输入 Markdown，包含标题/列表/代码块 | 右侧预览即时渲染，格式正确 |
| 2 | 光标移动到不同段落 | 当前段落高亮，状态栏行列信息同步 |
| 3 | 快速输入大段文本 | 渲染无明显卡顿（节流生效） |

### 2. 文件管理
| # | 步骤 | 期望结果 |
| --- | ------ | ---------- |
| 1 | 新建 → 输入 → 关闭窗口 | 弹出未保存提示，可保存/放弃/取消 |
| 2 | 打开已有 Markdown | 文本/预览同步更新，状态栏显示路径，标题无 * |
| 3 | 修改后保存/另存为 | 状态栏显示保存路径，窗口标题去除 * |

### 3. 富文本快捷操作
| # | 步骤 | 期望结果 |
| --- | ------ | ---------- |
| 1 | 选中文本 → Ctrl+B / Ctrl+I | 文本被 * 或  包裹，预览同步 |
| 2 | 行内代码/代码块动作 | 插入正确包裹（` text  / `\ncode\n``） |
| 3 | 插入链接 | 弹出 URL 输入框，插入 label |
| 4 | 无序列表 | 单行或选中多行前均加上 - |

### 4. 主题 & 大纲
| # | 步骤 | 期望结果 |
| --- | ------ | ---------- |
| 1 | 切换 Light/Dark/Solarized | 编辑区与预览颜色立即变化，重启后保持 |
| 2 | 撰写含多个 # 标题的文档 | 大纲 Dock 显示层级缩进，点击跳转正确 |
| 3 | 光标移动至不同标题 | 大纲选中项随之变化 |

## 日志 & 未来方向
- 若发现问题，请在 issue 中附带操作步骤与截图。
- 后续可考虑：
- 使用 Qt Test 为渲染/文件模型写单元测试
- 引入 GUI 自动化（Squish、pytest-qt、Playwright）回归关键路径
- 将本清单转为 CI 可执行脚本（例如截图 diff、Markdown diff 测试）
